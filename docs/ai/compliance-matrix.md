# Compliance Matrix

> Machine-friendly pass/fail rules for auditing implementations against the architecture.

| id | area | statement | kind | owned_by | applies_to | source_docs | verification_hint | common_drift |
|----|------|-----------|------|----------|------------|-------------|-------------------|--------------|
| F-01 | foundation | System must consist of exactly four components: Memory, Agent Loop, Auth, Gateway | must | foundation | whole system | `docs/foundation-spec.md`; `docs/foundation-verification.md` | Enumerate runtime components and classify each one | tools or models treated as components |
| F-02 | foundation | System must expose exactly two external APIs: Gateway API and Model API | must | foundation | whole system | `docs/foundation-spec.md`; `docs/foundation-verification.md` | Trace all external integration points | internal Gateway-Engine handoff treated as public API |
| F-03 | foundation | Tools must not be treated as a component | must_not | foundation | tools, engine, memory, auth | `docs/foundation-spec.md` | Inspect docs and code for standalone tool subsystem ownership | tools promoted into fifth component |
| F-04 | foundation | Memory must remain the substrate with zero outward dependencies | invariant | foundation | memory, whole system | `docs/foundation-spec.md`; `docs/foundation-verification.md` | Check import and runtime dependency direction | Memory depends on other components |
| G-01 | gateway | Gateway must own conversation lifecycle | must | gateway | gateway | `docs/gateway-spec.md`; `docs/foundation-spec.md` | Inspect where conversations are loaded, created, appended, and listed | conversation state moved into Engine or process-local store |
| G-02 | gateway | Gateway must not choose models as an architectural responsibility | must_not | gateway | gateway, configuration | `docs/gateway-spec.md`; `docs/configuration-spec.md` | Inspect endpoints and request path for provider/model selection logic | provider/model preference logic owned by Gateway |
| G-03 | gateway | Gateway must access conversations through the approved Memory boundary | must | gateway | gateway, memory | `docs/gateway-spec.md`; `docs/memory-spec.md` | Search Gateway code for direct storage access | direct file or database access from Gateway |
| G-04 | gateway | Gateway conversation operations must use a dedicated conversation-store boundary or tool rather than a private in-process store | must | gateway | gateway, memory | `docs/gateway-spec.md`; `docs/memory-spec.md` | Inspect conversation create/load/append/list path | private store bypasses Memory contract |
| G-05 | client-gateway-contract | Client message requests must use the canonical external shape `{ content, metadata? }` rather than constructing the internal `messages + metadata` contract directly | contract | client-gateway-contract | clients, gateway | `docs/ai/client-gateway-contract.md` | Inspect client request body and Gateway normalization path | client fabricates internal contract |
| G-06 | client-gateway-contract | Conversation list and detail endpoints must return canonical envelopes rather than raw storage records | contract | client-gateway-contract | gateway, clients | `docs/ai/client-gateway-contract.md` | Inspect response bodies for `/conversations` and `/conversations/:id` | raw storage shape leaked externally |
| G-07 | client-gateway-contract | Streaming responses must include `X-Conversation-ID` when the conversation is resolved and `done` must include `conversation_id` and `message_id` | contract | client-gateway-contract | gateway, clients | `docs/ai/client-gateway-contract.md` | Inspect response headers and `done` payload | completion lacks stable identifiers |
| E-01 | engine | Engine must remain a generic loop without product-specific logic | must | engine | engine | `docs/engine-spec.md` | Inspect loop code for product-specific prompt or business logic | Engine owns product workflow |
| E-02 | engine | Engine must not own conversation persistence | must_not | engine | engine, gateway | `docs/engine-spec.md`; `docs/foundation-spec.md` | Search Engine code for conversation store access | loop stores or truncates conversations |
| E-03 | engine | Provider-specific logic must stay in adapters, not core Engine flow | must | models | engine, adapters | `docs/models-spec.md`; `docs/adapter-spec.md`; `docs/engine-spec.md` | Inspect Engine for provider-specific request formatting | provider logic leaked into Engine |
| E-04 | engine | Engine must continue the loop until the model signals completion through the contract-defined finish behavior | must | engine | engine | `docs/engine-spec.md`; `docs/gateway-engine-contract.md` | Inspect loop termination conditions and safety caps | premature loop exit before model completion |
| C-01 | configuration | Runtime config, adapter config, and preferences must remain distinct | invariant | configuration | whole system | `docs/configuration-spec.md`; `docs/adapter-spec.md` | Inspect loaders, schemas, and preference storage paths | all config collapsed into one layer |
| C-02 | configuration | Request-time payloads must not become a hidden configuration channel | must_not | configuration | gateway, engine | `docs/configuration-spec.md`; `docs/gateway-engine-contract.md` | Inspect internal request metadata for config-like fields | per-request reconfiguration drift |
| C-03 | configuration | Secrets must not appear in Memory or tracked config files | must_not | configuration | whole system | `docs/configuration-spec.md` | Inspect config and Memory writes for secret values | secrets stored by value in config or preferences |
| M-01 | memory | Memory must have zero outward dependencies on Gateway, Engine, or Auth | invariant | memory | memory | `docs/memory-spec.md`; `docs/foundation-verification.md` | Check imports and runtime coupling from Memory outward | Memory depends on other components |
| M-02 | memory | Components must access Memory through approved tool or boundary paths, not direct storage reach-in | must | memory | gateway, engine, auth | `docs/memory-spec.md`; `docs/gateway-spec.md` | Search for direct filesystem or storage access in components | direct storage access from Gateway or Engine |
| M-03 | memory | Memory export must preserve required owner data in open formats | must | memory | memory | `docs/memory-spec.md`; `docs/foundation-verification.md` | Inspect export implementation and exported contents | incomplete export surface |
| M-04 | memory | Memory history must support change records and prior states as required by the contract | must | memory | memory | `docs/memory-spec.md` | Inspect history output and restore behavior | metadata-only history or no previous states |
| M-05 | memory | Conversations and structured/queryable owner data must remain inside the Memory contract rather than living as side storage outside it | must | memory | memory, gateway | `docs/memory-spec.md`; `docs/gateway-spec.md` | Inspect where conversations and structured data live and how they are exposed | conversation or structured data lives outside Memory surface |
| M-06 | memory | The model-facing Memory surface must expose required Memory storage classes through tools, even when multiple backends sit behind it | must | memory | memory, engine | `docs/memory-spec.md` | Inspect tool surface for file and structured/queryable access where required | database-backed Memory hidden behind non-Memory surfaces |
| A-01 | auth | Every protected request must pass through the Auth boundary before system interaction | must | auth | gateway, engine | `docs/auth-spec.md`; `docs/gateway-engine-contract.md` | Inspect runtime path and middleware or boundary wiring | auth omitted or bypassed |
| A-02 | auth | Auth must enforce access as an independent boundary rather than scattering authorization logic across components | must | auth | gateway, engine, tools | `docs/auth-spec.md`; `docs/foundation-spec.md` | Inspect where allow or deny decisions are made | ad hoc authorization in Gateway or Engine |
| A-03 | auth | Auth state must remain inspectable and exportable in product-owned data formats | must | auth | auth, memory | `docs/auth-spec.md` | Inspect identity, policy, and export data surfaces | opaque provider-owned auth state |
| A-04 | auth | Auth must not store secrets in normal inspectable Memory files | must_not | auth | auth, memory | `docs/auth-spec.md`; `docs/security-spec.md`; `docs/configuration-spec.md` | Inspect where tokens, hashes, and credentials are persisted | secrets written into Memory or preferences |
| A-05 | startup-readiness | MVP startup must create or load product-owned auth state sufficient for `auth_export` without embedding credentials in inspectable Memory | must | auth | auth, memory, startup | `docs/ai/auth.md`; `docs/ai/client-gateway-contract.md` | Inspect first-run auth-state initialization and export behavior | runtime auth works but no product-owned exportable state |
| S-01 | security | Exposed error and event surfaces must not leak unsafe internal details | must | security | gateway, engine | `docs/security-spec.md`; `docs/gateway-engine-contract.md` | Inspect streamed and client-visible errors for sanitization | raw runtime or provider errors forwarded |
| S-02 | security | Sensitive actions must be captured in the audit log using a structured format | must | security | auth, gateway, engine, memory, tools | `docs/security-spec.md` | Inspect audit sink and event coverage | missing or partial audit capture |
| S-03 | security | Approval enforcement must be implemented as a real control, not only prompt guidance | must | security | engine, tools, auth | `docs/security-spec.md`; `docs/tools-spec.md` | Inspect write path for coded approval interception | prompt-only approval policy |
| S-04 | security | Tool isolation and scope limits must enforce boundaries independent of Auth policy alone | must | security | tools, engine | `docs/security-spec.md` | Inspect tool runtime isolation and configured scope | full trust in auth with no tool-level boundary |
| S-05 | client-gateway-contract | Approval must appear in the client-facing contract through `approval-request` and `approval-result` rather than only local UI control flow | contract | client-gateway-contract | gateway, clients, engine | `docs/ai/client-gateway-contract.md`; `docs/gateway-engine-contract.md` | Inspect stream events and client handling | approval exists only in terminal-local flow |
| MO-01 | models | Models must remain external intelligence accessed through the Model API | invariant | models | engine, adapters | `docs/models-spec.md`; `docs/foundation-spec.md` | Inspect whether model access goes through the model boundary | models treated as an internal component |
| MO-02 | models | Same-provider model swaps must require only a preference change | must | models | models, configuration | `docs/models-spec.md`; `docs/adapter-spec.md`; `docs/configuration-spec.md` | Inspect model selection flow and preferences | model swap requires code or unrelated config edits |
| MO-03 | models | Provider swaps must require adapter and config changes, not unrelated component code changes | must | models | models, configuration, adapters | `docs/models-spec.md`; `docs/adapter-spec.md` | Inspect provider swap path and changed surfaces | provider choice hardcoded in Engine or Gateway |
| T-01 | tools | Tools must not be treated as a component or separate API | must_not | tools | tools, engine, whole system | `docs/tools-spec.md`; `docs/foundation-spec.md` | Inspect architecture docs and code for tool subsystem boundaries | tools promoted into a component or API |
| T-02 | tools | Tool execution must occur in the Agent Loop while authorization remains in Auth | must | tools | engine, auth, tools | `docs/tools-spec.md`; `docs/auth-spec.md` | Inspect tool call path for execution and authorization ownership | Gateway or tools enforce execution flow themselves |
| T-03 | tools | Tool availability must be determined by environment/config plus Auth permissions, not by ad hoc component logic | must | tools | tools, configuration, auth | `docs/tools-spec.md`; `docs/configuration-spec.md` | Inspect availability and discovery flow | bespoke availability logic inside product code |
| D-01 | deployment | The system must run on hardware the owner physically controls | must | deployment | whole system | `docs/deployment-spec.md` | Inspect deployment assumptions and local install path | remote infrastructure required to run |
| D-02 | deployment | A fully offline functional path must exist | must | deployment | whole system | `docs/deployment-spec.md` | Run or inspect local-model offline path | internet or cloud model required for basic loop |
| D-03 | deployment | Default network posture must be localhost-only | must | deployment | gateway, deployment | `docs/deployment-spec.md` | Inspect default bind addresses and exposure config | network exposure enabled by default |
| D-04 | deployment | The system must deploy as a single unit by default | must | deployment | whole system | `docs/deployment-spec.md` | Inspect default packaging and start path | multiple required services for baseline deployment |
| D-05 | startup-readiness | Startup must fail clearly if version history cannot be established rather than degrading to warning-only behavior | must | startup-readiness | memory, startup | `docs/ai/memory.md`; `spec/mvp-build-primer.md` | Inspect startup init path and git failure handling | best-effort version history startup |
| D-06 | startup-readiness | Startup must load runtime config, adapter config, tools, Memory, and preferences in deterministic order before the system is considered ready | must | startup-readiness | startup, configuration | `docs/ai/configuration.md` | Inspect startup sequence and readiness checks | partial or out-of-order startup creates hidden drift |
| D-07 | startup-readiness | Startup must make the export surface and conversation persistence path available from day one of MVP operation | must | startup-readiness | memory, gateway, startup | `docs/ai/memory.md`; `docs/ai/gateway.md`; `docs/ai/accepted-mvp-limits.md` | Inspect first-run behavior for export and persistent conversation support | MVP boots without required Memory guarantees |
| CU-01 | customization | Customization must happen through Memory content, tools, configuration, or client, not by changing generic component responsibilities | must | customization | whole system | `docs/customization-spec.md`; `docs/foundation-spec.md` | Inspect where product variation is implemented | product logic pushed into generic components |
| CU-02 | customization | The only fixed external contracts remain the Gateway API and Model API | invariant | customization | gateway, models, clients | `docs/customization-spec.md`; `docs/foundation-spec.md` | Inspect proposed customizations for contract breakage | new custom API or contract introduced as default |
| CU-03 | customization | Implementations should build on the architecture as a dependency relationship rather than a fork when generic mechanisms suffice | must | customization | whole system | `docs/customization-spec.md` | Inspect whether changes could have been content/config/tools/client instead of a fork | unnecessary architecture fork |
| GC-01 | gateway-engine-contract | Gateway must send only bounded `messages + metadata` data to Engine | contract | gateway-engine-contract | gateway, engine | `docs/gateway-engine-contract.md` | Inspect internal request shape and tests | config leaks through metadata |
| GC-02 | gateway-engine-contract | Engine must return stream events using the defined event types | contract | gateway-engine-contract | engine, gateway | `docs/gateway-engine-contract.md` | Inspect SSE emission and stream tests | off-contract event names or missing event classes |
| GC-03 | gateway-engine-contract | Mid-stream failures must use classified `error` events with safe messages | contract | gateway-engine-contract | engine, gateway | `docs/gateway-engine-contract.md`; `docs/security-spec.md` | Inspect error mapping and client-visible messages | raw errors forwarded or all failures collapsed to one code |
| GC-04 | gateway-engine-contract | Auth must sit on the path between Gateway and Engine | must | gateway-engine-contract | gateway, auth, engine | `docs/gateway-engine-contract.md`; `docs/auth-spec.md` | Inspect runtime path and middleware wiring | direct no-auth path to Engine |
| GC-05 | gateway-engine-contract | Mid-stream errors must use the contract taxonomy `provider_error`, `tool_error`, or `context_overflow` as appropriate | contract | gateway-engine-contract | engine, gateway | `docs/gateway-engine-contract.md` | Inspect emitted error codes and escalation paths | every failure collapsed to `provider_error` |
| GC-06 | gateway-engine-contract | Approval requests and outcomes must be represented through the contract interaction model rather than only a local client-specific prompt path | contract | gateway-engine-contract | gateway, engine, clients | `docs/gateway-engine-contract.md`; `docs/tools-spec.md` | Inspect whether approval appears in the stream or follow-up interaction contract | approval semantics trapped inside one UI or CLI |
